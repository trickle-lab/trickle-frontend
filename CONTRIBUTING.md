# Contributing to Trickle Frontend

## Setup
npm install
cp .env.local.example .env.local
(fill in your own values in .env.local)
npm run dev
Visit http://localhost:3000

## Verifying changes
npm run build

## Pull Requests
- Branch from main
- Run npm run build before pushing, CI will reject build errors